// Client-side demo goals + submissions for the "My Progress" page.
// Sourced from the shared mock clinic so all portals show the same numbers.
import { PRIMARY_GOALS, PRIMARY_LOGS } from "@/lib/mockClinic";

export const demoGoals = PRIMARY_GOALS;
export const demoLogs = PRIMARY_LOGS;